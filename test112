Make changes only in the **market** and **geofence** flow WHERE clauses.

Your event flow can stay optimized/dynamic.

---

## 1) Market flow change

### Replace this:

```sql
WHERE LOCATE(CONCAT(',', CAST(t.MARKET_ID AS CHAR), ','), CONCAT(',', IN_MARKET_CSV, ',')) > 0
```

### With this:

```sql
WHERE FIND_IN_SET(CAST(t.MARKET_ID AS CHAR), IN_MARKET_CSV) > 0
```

Do this in both places:

```sql
FROM V_F_SLEEPY_CELL_4G_H t
```

and

```sql
FROM V_F_SLEEPY_CELL_5G_H t
```

---

## 2) Geofence flow change

### Replace this:

```sql
WHERE LOCATE(CONCAT(',', t.GNODEB, ','), CONCAT(',', IN_GEOFENCE_NODE_CSV, ',')) > 0
```

### With this:

```sql
WHERE FIND_IN_SET(t.GNODEB, IN_GEOFENCE_NODE_CSV) > 0
```

Do this in both places:

```sql
FROM V_F_SLEEPY_CELL_4G_H t
```

and

```sql
FROM V_F_SLEEPY_CELL_5G_H t
```

---

## Final market 4G WHERE

```sql
FROM V_F_SLEEPY_CELL_4G_H t
WHERE FIND_IN_SET(CAST(t.MARKET_ID AS CHAR), IN_MARKET_CSV) > 0
  AND t.STARTDATE >= IN_START_DATE
  AND t.STARTDATE < DATE_ADD(IN_END_DATE, INTERVAL 1 DAY)
  AND t.SLEEPY_CELL_KAFKA_FLAG = 'Y'
  AND t.SLEEPY_CELLS_FINAL_FLAG = 'Y'
  AND (t.SEVERITY = IN_SEVERITY OR IN_SEVERITY IS NULL)
```

## Final market 5G WHERE

```sql
FROM V_F_SLEEPY_CELL_5G_H t
WHERE FIND_IN_SET(CAST(t.MARKET_ID AS CHAR), IN_MARKET_CSV) > 0
  AND t.STARTDATE >= IN_START_DATE
  AND t.STARTDATE < DATE_ADD(IN_END_DATE, INTERVAL 1 DAY)
  AND t.SLEEPY_CELLS_FINAL_FLAG = 'Y'
  AND (t.SEVERITY = IN_SEVERITY OR IN_SEVERITY IS NULL)
```

---

## Final geofence 4G WHERE

```sql
FROM V_F_SLEEPY_CELL_4G_H t
WHERE FIND_IN_SET(t.GNODEB, IN_GEOFENCE_NODE_CSV) > 0
  AND t.STARTDATE >= IN_START_DATE
  AND t.STARTDATE < DATE_ADD(IN_END_DATE, INTERVAL 1 DAY)
  AND t.SLEEPY_CELL_KAFKA_FLAG = 'Y'
  AND t.SLEEPY_CELLS_FINAL_FLAG = 'Y'
  AND (t.SEVERITY = IN_SEVERITY OR IN_SEVERITY IS NULL)
```

## Final geofence 5G WHERE

```sql
FROM V_F_SLEEPY_CELL_5G_H t
WHERE FIND_IN_SET(t.GNODEB, IN_GEOFENCE_NODE_CSV) > 0
  AND t.STARTDATE >= IN_START_DATE
  AND t.STARTDATE < DATE_ADD(IN_END_DATE, INTERVAL 1 DAY)
  AND t.SLEEPY_CELLS_FINAL_FLAG = 'Y'
  AND (t.SEVERITY = IN_SEVERITY OR IN_SEVERITY IS NULL)
```

---

No Java change is required.
